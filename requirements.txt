# Core Dependencies for Streamlit Frontend
streamlit>=1.28.0
pillow>=9.0.0
requests>=2.28.0

# Note: TensorFlow and model dependencies are now in the backend API
# The frontend only needs to communicate with the API

# For Training (Optional - only needed for notebook)
pandas>=1.3.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
seaborn>=0.11.0
tqdm>=4.62.0
